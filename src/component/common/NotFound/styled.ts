import styled from 'styled-components';

export const NotFoundStyled = styled.div`
    img {
        width: 100%;
        height: 100%;
        position: absolute;
        z-index: -1;
    }

    button {
        float: right;
    }
`;
